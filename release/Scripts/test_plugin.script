#ifdef properties
	#define script_name "test"
	#define is_plugin true
	#define plugin_target "FTLGame.exe"
#endif


#ifdef colour_plugin
	#define pixel_coord 365 60

	if BLACK
		trigger_once();
		audio("We took damage!");
		delay(2000);

	if RED
		audio("It's red!");
		key_send("it's red! ");
		delay(500);
#endif

